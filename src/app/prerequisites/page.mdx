import { Libraries } from '@/components/Libraries'

export const metadata = {
  title: 'NextStore Prerequisites',
  description:
    'NextStore offers fine-tuned JavaScript, Ruby, PHP, Python, and Go libraries to simplify the integration process and provide the best experience when interacting with the API.',
}

export const sections = [
  { title: 'Official libraries', id: 'official-libraries' },
]

# NextStore Prerequisites

The recommended way to interact with the NextStore API is by using one of our official NextJS. Currently, NextStore provides fine-tuned JavaScript and Typescript libraries to streamline your development process and give you the best experience when consuming the API. {{ className: 'lead' }}

<Libraries />
